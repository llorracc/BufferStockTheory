# CLAUDE.md

This file is for **Claude Code-specific** guidance only. All tool-agnostic guidance for this repo lives in [`AGENTS.md`](AGENTS.md) and [`.agents/`](.agents/) — read those first.

- Repository purpose, layout, build commands, architecture, privacy boundary, gotchas → see `AGENTS.md` and `.agents/{commands,architecture,gotchas}.md`.
- Anything below this line is specific to Claude Code's harness (skills, slash commands, memory, hooks).

## Claude Code specifics

*(none yet — add Claude-only guidance here as it arises; do not duplicate content from `.agents/`)*
